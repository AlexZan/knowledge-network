# Anomaly Ledger
#
# Track recurring issues across sessions. Each entry has:
#   category:    Short classification (llm-output, test-flake, perf, data, etc.)
#   description: What happened
#   count:       How many times observed
#   first_seen:  Date first observed
#   last_seen:   Date most recently observed
#   context:     List of occurrence notes (keep recent — trim to last 5)
#
# If you see something that matches an existing entry, increment count +
# update last_seen + append to context. Don't create duplicates.

- id: llm-json-truncation
  category: llm-output
  description: >
    LLM returns truncated/malformed JSON during chunk extraction, causing
    a JSONDecodeError. The chunk content is valid and short — the LLM just
    produces incomplete output. Fixed in _parse_llm_json(): strips control
    chars, repairs truncated arrays by closing at last complete object.
  count: 4
  first_seen: 2026-03-03
  last_seen: 2026-03-05
  github_issue: 7
  context:
    - "2026-03-03: ingest_pipeline on conflict-resolution-findings.md — chunk
      'Resolution 3: Explicit declaration vs. roundtable winner-picking'
      (~7 lines) failed with JSON parse error at char 220. 1/19 chunks
      affected (5% failure rate). Model: cerebras/gpt-oss-120b."
    - "2026-03-04: ingest_pipeline on BIG-PICTURE.md — chunk
      'big-picture-roadmap-phase-1-knowledge-network-python-prototype'
      failed with 'Invalid control character at: line 35 column 8 (char 1562)'.
      1/9 chunks affected. Model: cerebras/gpt-oss-120b. Fix: added control
      character sanitization (re.sub) before json.loads() in extract_from_chunk()."
    - "2026-03-05: physics KG re-ingestion — turn-16 of 6813ea48 failed with
      'Expecting comma delimiter at char 2420'. 1/111 chunks (0.9%).
      Model: cerebras/gpt-oss-120b. Non-deterministic (retry succeeded).
      Fix: added missing-comma repair ('}\\s*{' → '},{') to _parse_llm_json()."
    - "2026-03-05: SEP QT article ingestion — chunk 'entanglement-nonlocality'
      failed with 'Expecting property name enclosed in double quotes at char 1359'.
      1/22 chunks (4.5%). Model: cerebras/gpt-oss-120b. New failure mode:
      unquoted/malformed property names. Not yet handled by _parse_llm_json()."
