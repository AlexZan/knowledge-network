# Moved

Pipeline definitions have moved to the `oi` repo:

- `tdd-pipeline.md` → `D:\Dev\oi\pipeline\docs\tdd-pipeline.md`

This repo (knowledge-network) focuses on context/memory primitives.
Pipeline implementation lives in `oi/`.
