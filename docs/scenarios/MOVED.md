# Moved

Pipeline-related scenarios have moved to the `oi` repo:

- `peer-agent-scenario.md` → `D:\Dev\oi\pipeline\docs\peer-agent-scenario.md`

This repo (knowledge-network) focuses on context/memory primitives.
Pipeline implementation lives in `oi/`.
